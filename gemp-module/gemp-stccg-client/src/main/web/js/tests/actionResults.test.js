import {animateActionResult, communicateActionResult, getActionTargetCard, getSpacelineIndexFromLocationId} from '../gemp-022/actionResults.js';

test('actionResults.js is valid syntax', () => {
    // noop
});